# Calendar

Static React reservation calendar.

Public app URL after GitHub Pages deploy:

```text
https://martin9020.github.io/calendar/
```

Reservations are stored in the browser with `localStorage`. This makes the app work without a server, but data is local to each browser/device.
